import Link from "next/link";import type { ReleaseInfo } from "@/lib/validation/releases";
export function bytes(size:number){return `${(size/1024/1024).toFixed(1)} MB`}
export function ReleaseCard({release,compact=false}:{release:ReleaseInfo;compact?:boolean}){return <article className="panel p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="eyebrow">Latest stable release</p><h2 className="mt-2 text-3xl font-black">NowTuneUp {release.version}</h2><p className="muted mt-2">{release.apkName} · {bytes(release.apkSize)} · {release.minimumAndroid}+</p></div><span className="rounded-full border border-emerald-400/40 bg-emerald-400/10 px-3 py-1 text-sm text-emerald-300">Stable</span></div>{!compact&&<p className="mt-5 whitespace-pre-line text-slate-300">{release.releaseNotes}</p>}<div className="mt-6 flex flex-wrap gap-3"><Link className="button" href={`/api/downloads?version=${encodeURIComponent(release.version)}`} aria-label={`Download NowTuneUp ${release.version} APK`}>Download APK</Link><Link className="button secondary" href={`/releases/${encodeURIComponent(release.version)}`}>Release details</Link></div></article>}
