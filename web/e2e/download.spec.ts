import{test,expect}from"@playwright/test";test("homepage exposes the official download flow",async({page})=>{await page.goto("/");await expect(page.getByRole("heading",{name:/Real-time vehicle monitoring/})).toBeVisible();await page.getByRole("link",{name:"Download NowTuneUp"}).click();await expect(page).toHaveURL(/\/download/);await expect(page.getByRole("heading",{name:"Download NowTuneUp"})).toBeVisible()});
