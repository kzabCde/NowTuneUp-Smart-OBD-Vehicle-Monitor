import type{MetadataRoute}from"next";export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL??"https://nowtuneup.com";return['','/download','/releases','/features','/install-guide','/supported-devices','/privacy','/terms'].map((path):MetadataRoute.Sitemap[number]=>({url:`${base}${path}`,changeFrequency:path==='/download'?'daily':'monthly',priority:path===''?1:.7}))}
