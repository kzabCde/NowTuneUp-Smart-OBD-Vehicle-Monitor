import type{Metadata}from"next";export const metadata:Metadata={title:"Features"};const groups=[['Live dashboard','Responsive portrait and landscape gauges for critical readings.'],['All supported data','Search current Mode 01 values; unsupported PIDs are never shown as zero.'],['Read-only diagnostics','Read stored trouble codes with raw ECU responses and generic descriptions.'],['Trips and CSV','Record local telemetry sessions and export portable CSV files.'],['Custom layouts','Choose gauges, meters, cards and charts; save dashboard profiles.'],['Background monitoring','Continue a selected monitoring session with a visible foreground notification.']];export default function Features(){return <section className="shell py-16"><p className="eyebrow">Capabilities</p><h1 className="mt-3 text-5xl font-black">Vehicle data you can understand.</h1><div className="grid mt-9 md:grid-cols-2 lg:grid-cols-3">{groups.map(([t,d])=><article className="panel p-6" key={t}><h2 className="text-xl font-bold">{t}</h2><p className="muted mt-3">{d}</p></article>)}</div></section>}
