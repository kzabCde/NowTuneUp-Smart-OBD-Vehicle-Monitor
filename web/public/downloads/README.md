Place the current signed Android APK in this folder with the exact filename:

NowTuneUp.apk

When the app is updated, replace that file, update web/lib/local-release.ts, commit, and push to main. Vercel will redeploy the website automatically.
