name: Build bundled website APK

on:
  workflow_dispatch:
  push:
    branches:
      - 'release/**'
    paths:
      - android-app/**
      - .github/workflows/build-bundled-apk.yml

permissions:
  contents: write

concurrency:
  group: bundled-apk-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-commit:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - uses: android-actions/setup-android@v3

      - name: Sync in-app version label
        run: |
          sed -Ei 's/NTU [0-9]+\.[0-9]+\.[0-9]+/NTU 1.3.1/g' android-app/app/src/main/java/com/nowtuneup/app/MainActivity.kt

      - name: Build and sign APK 1.3.1
        working-directory: android-app
        env:
          NTU_KEYSTORE_FILE: ${{ github.workspace }}/android-app/keystore/nowtuneup-web-release.jks
          NTU_KEYSTORE_PASSWORD: NowTuneUpWeb100
          NTU_KEY_ALIAS: nowtuneup-web
          NTU_KEY_PASSWORD: NowTuneUpWeb100
        run: |
          chmod +x gradlew
          ./gradlew test lint assembleRelease

      - name: Verify and bundle APK with website
        run: |
          set -euo pipefail
          SOURCE="android-app/app/build/outputs/apk/release/NowTuneUp-v1.3.1-release.apk"
          TARGET="web/public/downloads/NowTuneUp.apk"
          test -s "$SOURCE"
          unzip -t "$SOURCE" >/dev/null
          APKSIGNER="$(find "${ANDROID_HOME}/build-tools" -type f -name apksigner | sort -V | tail -1)"
          test -n "$APKSIGNER"
          "$APKSIGNER" verify --verbose "$SOURCE"
          mkdir -p "$(dirname "$TARGET")"
          cp "$SOURCE" "$TARGET"
          SHA256="$(sha256sum "$TARGET" | awk '{print $1}')"
          SIZE="$(stat -c%s "$TARGET")"
          PUBLISHED_AT="$(date -u +%Y-%m-%dT%H:%M:%S.000Z)"
          python3 - "$SHA256" "$SIZE" "$PUBLISHED_AT" <<'PY'
          from pathlib import Path
          import re
          import sys

          sha256, size, published_at = sys.argv[1], int(sys.argv[2]), sys.argv[3]
          path = Path("web/lib/local-release.ts")
          text = path.read_text()
          text = re.sub(r'version: "[^"]+",', 'version: "1.3.1",', text)
          text = re.sub(r'tagName: "[^"]+",', 'tagName: "v1.3.1",', text)
          text = re.sub(r'versionCode: \d+,', 'versionCode: 7,', text)
          text = re.sub(r'apkSize: \d+,', f'apkSize: {size},', text)
          text = re.sub(r'sha256: (?:null|"[a-f0-9]{64}"),', f'sha256: "{sha256}",', text)
          text = re.sub(r'publishedAt: "[^"]+",', f'publishedAt: "{published_at}",', text)
          text = re.sub(
              r'releaseNotes: "[^"]*",',
              'releaseNotes: "Adds Gauge Focus Mode with tap-to-hide controls, touch lock, keep-screen-on, automatic focus, swipeable dashboard pages and compact connection status. Adds Peak/Min/Max, threshold alerts with sound, vibration, hysteresis and cooldown, data freshness and stale-value protection, automatic reconnect and last-dashboard resume.",',
              text,
          )
          path.write_text(text)
          PY
          test "$(sha256sum "$TARGET" | awk '{print $1}')" = "$SHA256"

      - name: Commit generated APK
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -f \
            android-app/app/src/main/java/com/nowtuneup/app/MainActivity.kt \
            web/public/downloads/NowTuneUp.apk \
            web/lib/local-release.ts
          if git diff --cached --quiet; then
            echo "Bundled APK is already current."
            exit 0
          fi
          git commit -m "Bundle NowTuneUp APK 1.3.1 with website"
          git push

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: NowTuneUp-1.3.1
          path: web/public/downloads/NowTuneUp.apk
          if-no-files-found: error
