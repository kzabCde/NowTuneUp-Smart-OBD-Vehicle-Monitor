name: Build bundled website APK

# Release build is temporarily manual while 1.6.2 source files are updated.
on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  waiting-for-release-source:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The 1.6.2 release workflow will be restored after source changes are complete."
